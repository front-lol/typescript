let any:any
let num:number
let str:string
let boo:boolean
let und:undefined
let nul:null
let obj:object
let sym:symbol

//모든 타입의 상위 타입은 any, 모든 타입의 하위 타입은 null,undefined

//new는 객체반환

let lis:string[]
lis=["s"]

//인라인 타입
let usr:{name:string,score:number}

usr = {
    name:'oy',
    score:2
}

let tuple2:[number,string]
tuple2=[1,'a']